apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: warehouse-network
  labels:
    app: web-app
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: web-app