{
  "name": "warehouse-network-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Warehouse Management Platform",
  "scripts": {
    "dev": "pnpm --filter @warehouse-network/web dev",
    "build": "pnpm --filter @warehouse-network/web build",
    "start": "pnpm --filter @warehouse-network/web start",
    "lint": "pnpm --filter @warehouse-network/web lint",
    "test": "pnpm --filter @warehouse-network/web test",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@8.6.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.6.1"
  },
  "dependencies": {
    "@radix-ui/react-label": "^2.1.8",
    "next-themes": "^0.4.6",
    "react-ga4": "^2.1.0"
  }
}
